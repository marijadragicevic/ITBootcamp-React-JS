import React from "react";
import spinner from "../../images/spinner.svg"

const Loading = () => {
    return (
        <div>
            <img src={spinner} />
        </div>
    )
}

export default Loading;